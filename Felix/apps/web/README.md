# @felix/web

SPA React 19 + Vite + TypeScript estrito do Felix Empire Trading.

- Tema Empire (dark, paleta de mesa de operacoes) via `@felix/ui`.
- Tipos compartilhados via `@felix/shared-types` (Zod).
- Roteamento, Auth (Supabase), TanStack Query, Zustand, i18n (pt-BR / en-US).

## Comandos

```
pnpm dev         # http://localhost:5173
pnpm build
pnpm test        # Vitest
pnpm test:e2e    # Playwright (após Etapa 8)
```

> A app real (dashboard, curso, simulado fullscreen, prova final, ranking, certificados, tutor) é construída a partir da Etapa 2.
